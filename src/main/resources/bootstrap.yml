spring:
  application:
    name: demo-client-service

server:
  port: 8111

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://172.16.254.190:8002/eureka