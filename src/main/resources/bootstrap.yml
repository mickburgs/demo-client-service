spring:
  application:
    name: demo-client-service

server:
  port: 8111

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://10.246.23.0:8002/eureka