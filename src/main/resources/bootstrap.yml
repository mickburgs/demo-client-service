spring:
  application:
    name: demo-client-service

server:
  port: 8111

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://172.31.2.12:31557/eureka